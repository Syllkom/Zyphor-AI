/**
 * Exporta objetos, funciones y otros recursos que se añaden a la instancia de conn.
 * como funciones y configuraciones, que serán accesibles en otras partes de la base.
 */

export default async () => { 
    /**... */
};
